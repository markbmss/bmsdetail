# Main Website

This folder contains the assets for the public BMS Detail website.

- `styles.css` contains the public site styling.
- `app.js` contains the public site interactions and animations.

The public HTML entry point remains at the repository root in `index.html`.
