# Client portal — separate Netlify site (clients.bmsdetail.com)
# Deploy this site with base directory set to "client-files-app" in Netlify dashboard
[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
